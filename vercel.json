{
  "version": 2,
  "functions": {
    "api/*.py": {
      "maxDuration": 10
    }
  },
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/public/$1" }
  ],
  "env": {
    "PYTHONPATH": "."
  }
}